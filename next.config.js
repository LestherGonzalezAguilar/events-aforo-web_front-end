/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ytimg.com", "images.unsplash.com", "datos.gob.es"]
  }
}

module.exports = nextConfig
